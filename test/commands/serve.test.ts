import "mocha";
import { expect } from "chai";
import * as TypeMoq from "typemoq";
import * as rewireMock from "rewiremock";

describe("commands:serve", () => {
  it("should start correctly", async () => {
    rewireMock.overrideEntryPoint("https", )
  });
});
